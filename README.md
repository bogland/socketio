socketio 연습

#깃 등록  
```
git init .  
git remote add origin https://github.com/bogland/socketio.git
git checkout master
git branch --set-upstream-to=origin/master master
git pull
git add *
git config --global user.email "guruwang2@gmail.com"
git commit -m "express 초기 세팅중"
git pull
```
